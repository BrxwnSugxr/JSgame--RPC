// declaring varaibles 
let win = 0;
let lose = 0;
let tie = 0;

let options = ['R', 'P', 'S'];
//creating a function to allow user to input "options" 
var playGame = function () {
  var userChoice = window.prompt('Enter R, P, or S:');
// if user doesnt input anything function exits 
  if (!userChoice) {
    return;
  }
// adding a contion of user input 




};
